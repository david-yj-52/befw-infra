# =============================================
# Kafka Configuration
# cp .env.example .env 후 실제 값 입력
# =============================================

# Kafka 브로커 외부 접속 주소 (로컬: localhost, 서버 배포 시: 서버 IP)
KAFKA_ADVERTISED_HOST=localhost

KAFKA_PORT=9092
KAFKA_UI_PORT=8989
